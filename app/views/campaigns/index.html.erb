<h1>Listing campaigns</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @campaigns.each do |campaign| %>
  <tr>
    <td style="padding-right: 20px;"><%= campaign.title %></td>
	<td><%= link_to 'Notes', campaign_notes_path(campaign)%></td>
	<td style="padding-right: 20px;"><%= link_to 'Loot', campaign_loots_path(:campaign_id=>campaign)%>&nbsp;&nbsp;&nbsp;</td>
    <td><%= link_to 'Show', campaign %></td>
    <td><%= link_to 'Edit', edit_campaign_path(campaign) %></td>
    <td><%= link_to 'Destroy', campaign, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Campaign', new_campaign_path %>
